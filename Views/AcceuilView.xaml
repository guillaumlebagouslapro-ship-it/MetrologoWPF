<UserControl x:Class="Metrologo.Views.AccueilView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Padding="5">
            <Button Command="{Binding ExecuterMesureCommand}" Content="▶ Exécuter" Margin="5,0"/>
            <Separator />
            <Button Command="{Binding StopperMesureCommand}" Content="⏹ Stop" Margin="5,0"/>
            <Separator />
            <Button Command="{Binding OuvrirConfigurationCommand}" Content="⚙ Configuration" Margin="5,0"/>
            <Button Content="⚙ Rubidium" Margin="5,0"/>
            <Button Content="📊 Param. Incertitudes" Margin="5,0"/>
        </ToolBar>

        <GroupBox Grid.Row="1" Header="Informations Générales" Margin="0,10,0,0">
            <TextBox Text="{Binding InformationsGenerales}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas"/>
        </GroupBox>
    </Grid>
</UserControl>