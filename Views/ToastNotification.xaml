<Window x:Class="Metrologo.Views.ToastNotification"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="380"
        Focusable="False">
    <Border Background="#FF13294B" CornerRadius="10" Margin="12" Padding="16,12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.4" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Width="32" Height="32" CornerRadius="16"
                    Background="#FF16A34A" Margin="0,0,12,0" VerticalAlignment="Top">
                <TextBlock Text="✓" FontSize="18" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1">
                <TextBlock x:Name="TxtTitre" Text="Changement pris en compte"
                           FontSize="13" FontWeight="Bold" Foreground="White"
                           TextWrapping="Wrap"/>
                <TextBlock x:Name="TxtMessage" FontSize="12" Margin="0,3,0,0"
                           Foreground="#FFCBD5E1" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
